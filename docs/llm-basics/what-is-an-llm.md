# What is an LLM

_Placeholder._ A large language model is a neural network trained to predict the next token in a sequence. Given a prompt, it returns a probability distribution over the next token; we sample from that distribution repeatedly to generate text.
